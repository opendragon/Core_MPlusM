* the running sum adapter command set should include start / stop / quit / reset

* use selected inclusion of ODEnableLogging/ODLogging, ODDisableLogging/ODLogging to bracket code that needs to be logged

* make sure that the constructors for the BailOut are given the channel value

* change the BailOut calls to use a single ‘do while’ loop so that only one BailOut object is needed, and don’t allocate it on the heap